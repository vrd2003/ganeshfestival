{
  "name": "ganeshfestival",
  "private": true,
  "workspaces": ["client"],
  "scripts": {
    "build": "npm run build --workspace client",
    "test": "npm test --workspace client -- --watchAll=false"
  }
}
